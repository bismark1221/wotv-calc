import { WINDOW, WindowTokenModule } from './ngx-window-token';
import { TestBed, inject } from '@angular/core/testing';
describe('Service token: WINDOW', function () {
    beforeEach(function () {
        TestBed.configureTestingModule({
            imports: [WindowTokenModule]
        });
    });
    it('should service work', inject([WINDOW], function (window) {
        expect(window).toBeTruthy();
    }));
});
//# sourceMappingURL=ngx-window-token.spec.js.map